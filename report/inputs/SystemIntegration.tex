\subsection{Integration Strategy and Version Control}

From the very beginning, the team adopted a modular approach, assigning each collaborator one of three distinct modules to optimize parallel development; the modules were then integrated via ROS topic communication channels to determine the current state of the robot.

\paragraph{Launch Files \& Node Orchestration.}
A single composable launch file (`system.launch.py`) instantiates the three nodes and wires the remappings shown in Table~\ref{tab:channels}. This guarantees that every developer runs the exact same graph with one command:
\begingroup\small
\begin{verbatim}
ros2 launch orbit_tower s1.launch name:=/rm0
\end{verbatim}
\endgroup

\paragraph{Git Workflow}
The repository uses a single protected \texttt{main} branch. Collaborators create feature branches off of \texttt{main}, naming them as \texttt{<username>/<keyword>} to indicate the task or feature under development. Once work is complete, changes are merged back into \texttt{main} via pull requests.

\subsection{ROS 2 Node Graph \& Communication Channels}
The communication between the three ROS2 nodes in the system — \texttt{MockMapPublisher}, \texttt{PathPlannerNode}, and \texttt{ControllerNode} — is established through various ROS topics. Here's an overview of their coordination logic:\\
At initialization, the \texttt{MockMapPublisher} and \texttt{PathPlannerNode} nodes sets the internal flag \texttt{self.go\_again = True} and the \texttt{ControllerNode} sets \texttt{self.goal\_reached = False}.
These initial states allow both the \texttt{MockMapPublisher} and \texttt{PathPlannerNode} to begin processing immediately, while the \texttt{ControllerNode} waits for confirmation that a goal has been reached.
During operation, once the \texttt{PathPlannerNode} reaches its goal, it publishes a velocity command \texttt{cmd\_vel = (0.0, 0.0)} to bring the robot to a halt. Then publishes the following message to notify other nodes of goal completion:
\[
    \texttt{self.pub\_goal\_reached.publish(Bool(data=True))}
\]
and sets $\texttt{self.go\_again = False}$ to indicate that no further planning is needed until explicitly requested again.
The \texttt{ControllerNode}, which subscribes to the \texttt{/goal\_reached} topic, receives this message and updates its internal flag ($\texttt{self.goal\_reached = True}$). Upon completing its own task (such as analyzing sensor data or interacting with the tower), it publishes:
\[
\texttt{self.turn\_ended.publish(Bool(data=True))}
\]
on the topic \texttt{/go\_again}.
Both the \texttt{MockMapPublisher} and \texttt{PathPlannerNode}, which subscribe to \texttt{/go\_again}, receive this signal and set $\texttt{self.go\_again = True}$.
This loop allows the system to autonomously progress through a series of goals or states in a coordinated manner.
This design ensures that the map publisher and planner are event-driven and coordinated with the controller logic, enabling robust and synchronized robot behavior across discrete tasks.

\begin{table*}[h]
  \centering
    \resizebox{\textwidth}{!}{
    \begin{tabular}{@{} l l l l l @{}} 
    \toprule
    \textbf{Function} & \textbf{Topic} & \textbf{Type} & \textbf{Publisher} & \textbf{User} \\
    \midrule
    2D Map           & \texttt{/map}                       & \texttt{nav\_msgs/OccupancyGrid} & MockMapPublisher             & Planner       \\
    Odometry         & \texttt{/odom}                      & \texttt{nav\_msgs/Odometry}      & Robo (sim)                   & Planner       \\
    Current Pose     & \texttt{/current\_pose}             & \texttt{geometry\_msgs/PoseStamped} & Planner                  & Robot         \\
    Goal             & \texttt{/goal\_pose}                & \texttt{geometry\_msgs/PoseStamped} & MockMapPublisher / RViz  & Planner       \\
    Trajectory       & \texttt{/plan}                      & \texttt{nav\_msgs/Path}          & Planner                      & (debug)       \\
    Velocity Commands& \texttt{/rm0/cmd\_vel}              & \texttt{geometry\_msgs/Twist}    & Planner                      & Robot         \\
    Goal Reached     & \texttt{/goal\_reached}             & \texttt{std\_msgs/Bool}          & Planner                      & MockMapPublisher \\
    Resume Path      & \texttt{/go\_again}                 & \texttt{std\_msgs/Bool}          & MockMapPublisher             & Planner       \\
    Velocity         & \texttt{/cmd\_vel}                  & \texttt{geometry\_msgs/Twist}    & ControllerNode               & Robo          \\
    Images           & \texttt{/rm0/camera/image\_color}   & \texttt{sensor\_msgs/Image}      & Robo (sim)                   & ControllerNode \\
    Range Sensing    & \texttt{/rm0/range\_[0-3]}          & \texttt{sensor\_msgs/Range}      & Robo (sim)                   & ControllerNode \\
    \bottomrule
  \end{tabular}
  }
  \caption{ROS~2 communication channels used by the system.}
  \label{tab:channels}
\end{table*}


\subsection{Observed Failure Modes \& Mitigations}
\begin{enumerate}
  \item \textbf{Duplicate \texttt{/goal\_reached} notifications.}\\
        Early versions latched the topic, causing every subscriber restart to retrigger the callback.  
        \emph{Fix:} publish as non‑latched and gate the callback with a rising‑edge detector.
  \item \textbf{Planner reading sensors out of turn.}\\
        While the robot executed a goal, the planner continued to process range data, leading to sporadic replanning.  
        \emph{Fix:} suspend sensor callbacks while \texttt{self.go\_again == False}.
  \item \textbf{Stale TF frames.}\\
        A mis‑configured static transform broadcaster produced frames older than the TF timeout, yielding large jumps in odometry.  
        \emph{Fix:} discard transforms older than 0.5\,s and add a health-check in CI.
\end{enumerate}